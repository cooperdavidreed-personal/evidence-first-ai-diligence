import {render,screen,fireEvent,waitFor,cleanup} from '@testing-library/react';
import {afterEach,expect,it,vi} from 'vitest';
import {DesktopOnboarding} from './desktop-onboarding';
afterEach(()=>{cleanup();vi.unstubAllGlobals();});
it('can finish without Claude, open the workspace and revisit setup without destroying work',async()=>{let completed=false;vi.stubGlobal('fetch',vi.fn(async(_url,options)=>{const action=options?.body&&JSON.parse(options.body).action;if(action==='complete')completed=true;return {ok:true,json:async()=>({completed,verified:false,version:'test'})};}));render(<DesktopOnboarding><input aria-label="Unsaved analyst note" defaultValue="Keep this"/></DesktopOnboarding>);await screen.findByRole('button',{name:'Continue without Claude'});await waitFor(()=>expect(screen.getByRole('button',{name:'Continue without Claude'})).toBeEnabled());fireEvent.click(screen.getByRole('button',{name:'Continue without Claude'}));const note=await screen.findByLabelText('Unsaved analyst note');fireEvent.change(note,{target:{value:'Still here'}});fireEvent.click(screen.getByRole('button',{name:'Connection setup'}));fireEvent.click(screen.getByRole('button',{name:'Continue without Claude'}));await waitFor(()=>expect(note).toBeVisible());expect(screen.getByLabelText('Unsaved analyst note')).toHaveValue('Still here');});
it('reports an unavailable local service and never claims verification',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>{throw Error('Local service unavailable');}));render(<DesktopOnboarding>Workspace</DesktopOnboarding>);expect(await screen.findByRole('alert')).toHaveTextContent('Local service unavailable');expect(screen.queryByText('Verified')).not.toBeInTheDocument();});
