# Workstation trial readiness — September 8

Cooper authorized completing trial preparation and pushing the accumulated implementation. Owner: Codex. Scope: this repository, branch `codex/desk-local-20260907`, including the previously local design and workflow increments. Shared company-state synchronization remains pending; the filesystem report is the handoff. No merge, production deployment, account change, paid inference or outbound message is part of this release.

## What is ready

The package creates a persistent investment workspace from incomplete company materials; Claude proposes cited findings and diligence through explicitly released MCP evidence; analysts adopt the case, track decision questions, review revised deliveries and maintain committee materials. It retains the existing PE/VC examples and engines. Arbitrary uploaded workbooks feed explicitly mapped operating evidence, not fabricated transaction calculations.

Navigation is Deals → Brief / Review / Model / Evidence / Committee. A fixed left rail, restrained workpaper styling, compact financial tables and adjacent source inspection support the workflow. This is one local workstation application, not a firm-wide enterprise platform.

The pushed `trial/` directory contains the exact tested ZIP, its checksum, plain setup instructions, two synthetic practice notes and a blank feedback record. It contains no analyst database or credentials. The ZIP is unsigned and requires permitted Node.js 24+ on the target machine. A branch-specific Vercel configuration suppresses automatic deployment of this trial branch; main remains separate.

## Release work in this pass

- Corrected connection setup to use `list_investment_reviews` and distinguish an extracted package from a source checkout.
- Updated CI's Node runtime from 22 to 24 to match the local package minimum.
- Rebuilt and exercised the extracted distribution, including paths with spaces, MCP evidence/proposals and encrypted backup/delete/restore.
- Exercised text downloads and the populated browser print document in both desktop engines.
- Removed device-specific paths from publishable notes/logs; retained exact artifact hashes and historical proof boundaries.
- Rebound the inherited visual manifest to its retained historical files. Added an explicit hash-bound allowlist for retained synthetic screenshots, the synthetic walkthrough and the trial ZIP. The ZIP inventory is checked against its own file manifest and scanned for private file types and text patterns. Hashes are integrity checks, not publisher signatures or a security audit.

## Verification and interpretation

Frontend: 240 tests passed. Server/MCP: 26 passed. Package tests: six passed. TypeScript, production build and chunk budgets passed; the lazy case/PDF size warning remains.

Desktop browser regression: 76 passed, 12 conditional skips. Local review/archive tests: two passed separately per Chromium and WebKit, four total. Unified company/revision/export workflows: four passed across both engines. A first exploratory run incorrectly combined browser-storage tests and multiple projects with one shared persistent database; it failed on shared fixture state and browser-only recovery expectations. The passing reruns use browser-only mode for that regression suite and separate disposable databases for local fixtures. This does not establish simultaneous multi-user support.

The system Python environment lacked the repository's required RFC3339 format validator and produced two date-error-contract failures. No investment input was accepted: the runtime still rejected those dates. Installing this checkout's locked development environment resolved the affected pipeline/publication suite: 30 tests passed. Full locked-suite result is recorded below when complete.

## External trial checks

Only the target workstation can establish installation permission, the organization's Claude Desktop local-MCP policy, and actual account connection. Local checks use a synthetic MCP client, not the brother's Claude Enterprise identity. Native Windows installation and this new change-table export in his Excel remain unverified. Browser print content is verified; his native PDF-saving dialog is a workstation check.

Claude Enterprise use does not itself authorize local MCP. Official reference checked during release preparation: https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop . Vercel branch control: https://vercel.com/docs/project-configuration/git-configuration .

Run the independent test in `docs/DESK-ANALYST-PILOT-20260909.md`. Measure setup, corrections, duplicate entry, time to usable output, and whether the analyst chooses to reuse it. Do not lead with more features before that evidence. Remaining product boundaries include no OCR/DOCX, no arbitrary-model recalculation, no firm-wide identity or collaboration, source-scoped context, and review-batch friction when workspace saves revoke the prior model release.

Native Excel check: the new synthetic change-table export opened in Microsoft Excel on macOS with worksheet Review changes, used range A1:I2 and Ready status, with no repair dialog observed. This establishes local opening compatibility, not his Windows setup or arbitrary model calculation. See trial-native-excel-open.json.

Minimum-runtime exercise: the same ZIP passed its extracted launcher/API/MCP/backup/delete/restore checks using Node v24.20.0, in addition to Node v26.3.0. The manifest remains unsigned.

## Final result

Full locked Python regression: **190/190 passed**, 775.28 seconds. No Python investment-engine change was needed. The earlier system-environment run was stopped after its missing format-validator dependency was diagnosed; its failure is not represented as a passing run.

Application/source candidate: `da1e0a730aaad9435dd5efa8f5312151cf87661b`. Pushed to `origin/codex/desk-local-20260907`; the subsequent verification-only commit records these results. Remote main remained `e866ca110da4b7c61c4543dd1b8135c6cb251ce6`. GitHub served the exact tested trial ZIP: 3,921,931 bytes, SHA-256 `2382e4387424fa800b81c7fc962426411268dc9fdb9a4e399eb11d227a3edc8f`.

Ready for the managed-workstation installation and independent analyst trial, subject to that device/account's actual permissions. Not yet practitioner-validated. All authorized source, design, documentation and trial-package work is pushed. No production merge or deployment was performed.
